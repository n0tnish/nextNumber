let numbers = [1, 2, 3, 4, 5];
let currentIndex = 0;
function displayNextElement() {
    if (currentIndex >= numbers.length) {
        currentIndex = 0;
        let nextElement = number[currentIndex];
    }
}